# Compatibility entry point. pyproject.toml is the dependency source of truth;
# requirements-lock.txt is generated with:
#   python -m piptools compile pyproject.toml -o requirements-lock.txt --strip-extras
-r requirements-lock.txt
